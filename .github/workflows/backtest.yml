name: Backtest

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: '開始日 (YYYYMMDD, 例: 20250101)'
        required: false
        default: ''
      end_date:
        description: '終了日 (YYYYMMDD, 例: 20251130)'
        required: false
        default: ''

jobs:
  backtest:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Run backtest
        run: |
          pip install requests --quiet
          python3 backtest.py "${{ github.event.inputs.start_date }}" "${{ github.event.inputs.end_date }}"

      - name: Commit results
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add backtest_result.csv backtest_summary.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "backtest result $(date +%Y%m%d)"
          git push
